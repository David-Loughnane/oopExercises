#include <iostream>
#include <cmath>

using namespace std;

int main() {

  long double a = 77617, b = 33096;
  long double f;

  f = (((333.75*pow(b,6) + pow(a,2)*(11*pow(a,2)*pow(b,2) - 121*pow(b,4))) - 5.5*pow(b,8)) - pow(a,2) * pow(b,6)) + (a/(2*b));

  cout << f << endl;

  return 0;
}
